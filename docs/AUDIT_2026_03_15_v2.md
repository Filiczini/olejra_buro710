# Buro 710 — Аудит v2

**Дата:** 2026-03-15
**Scope:** React 19 + Vite 7 + Tailwind v4 + Supabase + Express 5
**Контекст:** Проведено після закриття попереднього аудиту (40+ findings)

---

## 1. БЕЗПЕКА

| # | Проблема | Файл:рядок | Пріоритет | Статус |
|---|----------|-----------|-----------|--------|
| 1.1 | GET-роути `/api/posts/` та `/api/posts/:id` без `authMiddleware` — можна отримати список всіх постів (включно з чернетками) та будь-який пост за ID без авторизації | `backend/src/routes/posts.ts:32,76` | high | ✅ `optionalAuthMiddleware` для GET /, `authMiddleware` для GET /:id |
| 1.2 | Swagger UI (`/api/docs`) доступний у production без аутентифікації — документація API відкрита для всіх | `backend/src/index.ts:53-88` | medium | ✅ Swagger за `NODE_ENV !== 'production'` |

---

## 2. FRONTEND — БАГИ ТА АНТИ-ПАТЕРНИ

| # | Проблема | Файл:рядок | Пріоритет | Статус |
|---|----------|-----------|-----------|--------|
| 2.1 | Search useEffect циклічні залежності: `pagination.page` в deps + скидання page | `PostsPage.tsx:54-63` | high | ✅ `loadPosts(page)` приймає page як аргумент, прибрано з deps |
| 2.2 | Немає `ErrorBoundary` — runtime помилка краштить весь UI | `App.tsx` | high | ✅ ErrorBoundary обгортає App, логує через logger |
| 2.3 | `ActivityLogPage` дублює inline-пагінацію замість `Pagination` компонента | `ActivityLogPage.tsx:275-315` | medium | ✅ Замінено на `<Pagination />` компонент |
| 2.4 | `HeroSlider` — зображення без `loading="lazy"` | `HeroSlider.tsx:89-93` | medium | ✅ `loading={index === 0 ? 'eager' : 'lazy'}` |
| 2.5 | `FeaturedPostsSection.tsx` — мертвий код | `frontend/src/components/sections/` | medium | ✅ Файл видалено |
| 2.6 | `PublicPostPage` — `document.title` не скидається при unmount | `PublicPostPage.tsx` | low | ✅ Cleanup в useEffect |
| 2.7 | `Suspense fallback={null}` — порожня сторінка при lazy-завантаженні | `App.tsx` | low | ✅ Fallback з текстом "Завантаження..." |

---

## 3. BACKEND — ЯКІСТЬ КОДУ

| # | Проблема | Файл:рядок | Пріоритет | Статус |
|---|----------|-----------|-----------|--------|
| 3.1 | Health check не перевіряє з'єднання з БД | `index.ts:90-92` | medium | ✅ Перевірка Supabase, 503 при недоступності |
| 3.2 | `contactService` inconsistent pagination format | `contactService.ts` | low | ✅ Формат `{ data, pagination: {...} }` |
| 3.3 | GET routes повертають 404 на будь-яку помилку | `routes/posts.ts` | low | ✅ Перевірка `AppError` + правильний статус-код |

---

## 4. ТЕСТОВЕ ПОКРИТТЯ

| # | Проблема | Пріоритет | Статус |
|---|----------|-----------|--------|
| 4.1 | `storageService` — 0 тестів | high | ✅ 22 тести (upload/delete, success/error) |
| 4.2 | 5 backend-сервісів без тестів | medium | ⏳ Залишається (userService, activityLog, contact, telegram) |
| 4.3 | Frontend hooks без тестів | medium | ⏳ Залишається |
| 4.4 | Shared package без тестів | medium | ✅ 57 тестів (transliterate, generateSlug, Zod schemas) |

---

## 5. ДОКУМЕНТАЦІЯ

| # | Проблема | Файл | Пріоритет | Статус |
|---|----------|------|-----------|--------|
| 5.1 | `CLAUDE.md` з неіснуючими командами | `CLAUDE.md` | medium | ✅ Видалено `seed`, `migrate:projects` |
| 5.2 | Старий аудит-файл | `docs/AUDIT_2026_03_15.md` | low | ✅ Файл видалено |

---

## Зведення

| Пріоритет | Всього | Закрито | Залишилось |
|-----------|--------|---------|------------|
| **HIGH** | 4 | 4 | 0 |
| **MEDIUM** | 10 | 8 | 2 |
| **LOW** | 5 | 5 | 0 |
| **Всього** | 19 | 17 | 2 |

### Не закрито (2)
- **4.2** — Backend сервіси без тестів (userService, activityLogService, contactService, telegramService)
- **4.3** — Frontend hooks без тестів (useAuth, usePostForm, useFetchPosts)
