# Інструкція: Запуск міграцій

**Статус**: ✅ Готовий до виконання
**Мова**: 🇺🇦
**Час виконання**: ~10 хвилин

---

## 📋 Обов'язкова документація

| Документ | Опис |
|----------|------|
| **Main Guide** | `docs/deployment/deployment-verification.md` | Повний план деплою |
| **Implementation Plan** | `docs/implementation/dynamic-product-page-plan.md` | Детальний план реалізації |
| **Ukrainian Guide** | `docs/deployment/інструкція-міграція-динамічної-сторінки.md` | Цей файл! |

---

## 🚀 Крок 1: Міграція бази даних (5 хвилин)

### 1.1 Перейдіть в Supabase Dashboard

1. **Відкрийте проект**:
   - URL: https://supabase.com/dashboard/project/zrcaowbpewulytlwnbnb
   - Підтвердіть правильний проект

2. **Знайдіть SQL Editor**:
   - Натисніть: **SQL Editor** (ліворі меню)
   - Або натисніть комбінацію `Shift+Q`

3. **Створіть новий запит**:
   - Натисніть: **"New Query"**
   - Або натисніть комбінацію `Shift+Q`

### 1.2 Виконіть міграцію

1. **Вставте SQL міграції**:
   - Відкрийте файл: `003-add-project-sections.sql`
   - Скопіюйте весь вміст файлу (Ctrl+A або Cmd+A)

2. **Запустіть міграцію**:
   - Натисніть кнопку **"RUN"** (або F5)
   - Зачекайте повідомлення: `Success. No rows returned.`

3. **Перевірте результат**:

   ```
   Success. No rows returned.
   ```

   Це означає успіх!

### 1.3 Опціональна перевірка

В SQL Editor виконіть цей запит:

```sql
-- Перевірка: чи колонка існує та має правильний тип
SELECT id, title, sections
FROM projects
LIMIT 5;
```

**Очікувані результат**:
- Колонка `sections` повинна існувати
- Тип колонки: `jsonb`
- Значення: `[]` (пустий масив)

---

## 🚀 Крок 2: Міграція даних існуючих проектів (2 хвилини)

### 2.1 Переконте credentials

Перевірте, що в файлі `.env` є необхідні змінні:

```bash
# Перевірте credentials
cat .env | grep SUPABASE
```

Повинні бути:
```
SUPABASE_URL=https://zrcaowbpewulytlwnbnb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_...
```

### 2.2 Запустіть міграційний скрипт

У терміналі виконіть:

```bash
# Перейдіть в папку проекту
cd C:\CODE\buro710

# Запустіть міграцію
npx tsx scripts/migrate-project-sections.ts
```

### 2.3 Очікувані результат

Скрипт покаже:

```
🚀 Starting project sections migration...

📊 Found 22 projects to migrate

🔄 Migrating project: 6e11d6c3-3304-466f-a438-fc1bcfd3ead7
  ✅ Added hero section
  ✅ Added metadata section
  ✅ Added about section
  ✅ Added full-width image section
  ✅ Added concept section
✅ Successfully migrated project 6e11d6c3... with 6 sections

... (повторюється для всіх проектів)

============================================================
📈 МІГРАЦІЯ СУММА
============================================================
Total projects: 22
✅ Successfully migrated: 22
❌ Failed to migrate: 0
============================================================
✅ All projects migrated successfully!
```

### 2.4 Що створено для кожного проекту

Міграційний скрипт створює наступні секції:

| Секція | Порядок | Опис |
|-------|--------|------|
| **Hero Section** | 0 | Головна секція з картинкою, заголовком, підзаголовком |
| **Metadata Section** | 1 | Метадані проекту (архітектори, площа, рік, фото) |
| **About Section** | 2 | Опис проекту |
| **Full-Width Image** | 3 | Повноекранна картинка |
| **Concept Section** | 4 | Концепція дизайну з галереєю зображень |

---

## 🚀 Крок 3: Тестування адмін-панелі (10 хвилин)

### 3.1 Логін в систему

1. **URL адмін-панелі**:
   ```
   http://localhost:5173/admin/login
   ```

2. **Користувачі**:
   - Email: `admin@example.com`
   - Пароль: `admin123`

### 3.2 Перейдіть до адмінки

1. Натисніть: **"Dashboard"**
2. Знайдіть будь-який проект у списку

### 3.3 Відкрийте "Manage Sections"

Натисніть кнопку **"📋 Manage Sections"** на картці проекту

### 3.4 Тестуйте додавання секцій

1. **Кнопка "Add New Section"**:
   - Відкрийте вікно з 10+ типів секцій
   - Натисніть: `"Hero Section"`
   - Заповніть контент:
     - Title: `Мій новий заголовок`
     - Subtitle: `Архітектурний проект`
     - Short description: `Опис проекту коротко`
     - Image URL: `https://...`
   - Layout: `centered` (або `left`, `right`, `split`)

2. **Натисніть "Edit"**:
   - Відкриється редактор контенту
   - Заповніть дані
   - Натисніть "Close"
   - Секція зберігається

3. **Натисніть "Save Changes"**:
   - Зберігається в базу даних
   - З'являється повідомлення: `"Sections saved successfully!"`

4. Перейдіть назад на сторінку проекту:
   ```
   http://localhost:5173/project/[project-id]
   ```
   - Секції повинні відобразитися з новим контентом

### 3.5 Тестуйте редагування

1. **Відкрийте існуючу секцію**
2. Натисніть `"Edit"`
3. **Змініть**:
   - Title: `Оновлений заголовок`
4. Натисніть `"Close"`
5. Натисніть `"Save Changes"`
6. Поверніться на сторінку проекту

### 3.6 Тестуйте видалення

1. Натисніть кнопку `"Delete"`
2. Підтвердіть видалення в діалоговому вікні
3. Секція зникає зі списку
4. Збережіть зміни (`Save Changes` опціонально)

### 3.7 Тестуйте переміщування порядку

1. Знайдіть секцію, яку хочете перемістити вище
2. Натисніть кнопку `"↓"` (стрілка вниз)
3. Натисніть `"Save Changes"`
4. Перевірте новий порядок на сторінці проекту

### 3.8 Тестуйте toggle visibility

1. Натисніть кнопку `"Visible"` / `"Hidden"`
2. Секція показується/ховається на сторінці проекту
3. Збережіть зміни

---

## 📋 Крок 4: Додавання перекладів (опціонально, можна пізніше)

### 4.1 Як додати переклад

Після того, як секції створені:

1. **Відкрийте секцію для редагування**:
   - Натисніть `"Edit"`
   
2. **Знайдіть переклади**:
   - У нижній частині редактора є місце для перекладів
   - Поки що не реалізовано - переклади ще в розробці

3. **Заповніть переклад**:
   - Для кожної секції будуть поля для перекладів
   - Наприклад: `title_en`, `title_de` (для різних мов)

### 4.2 Структура перекладів

Планирується така структура:

```typescript
{
  translations: {
    "uk": {
      title: "Заголовок українською",
      content: { ... }
    },
    "en": {
      title: "Title in English",
      content: { ... }
    }
  }
}
```

---

## 🎯 Що може бути покращено (майбутнє)

### Функціонал, яка може бути додана пізніше:

1. **Visual Editors** (Замість JSON textarea):
   - Design Zones Builder з drag-and-drop
   - Gallery Builder з drag-and-drop та preview
   - Image Upload з preview та drag-drop

2. **Section Templates**:
   - Предналаштовані комбінації секцій
   - Шаблони для "About page", "Portfolio page", тощо

3. **Preview Mode**:
   - Живий превью змін на адмін-панелі
   - Порівняйте з версіями (before/after)

4. **Bulk Operations**:
   - "Enable All" / "Disable All"
   - "Delete All" (з підтвердженням)
   - "Duplicate Section" (копіювати секцію)

5. **Version History**:
   - Збереження попередніх версій секцій
   - Відновлення до попередньої версії
   - Порівняння версій

---

## 🚀 Поширення та підтримка

### Часті питання

**Q: Як додати новий тип секції?**
A: Це зроблено! Є 10 типів секцій. Якщо потрібен інший, додайте його в `SectionType` type та створіть компонент + редактор.

**Q: Як змінити порядок секцій?**
A: В адмін-панелі натисніть ↑↓ біля секції або перетягуйте (drag-and-drop в майбутньому).

**Q: Як приховати секцію без видалення?**
A: Натисніть кнопку "Visible/Hidden". Приховані секції не видаляються з бази.

**Q: Чому не бачу змін на сторінці проекту?**
A: Переконте, що натиснули "Save Changes" та перейдіть на сторінку проекту. Якщо змін не видно - перевірте console браузера на помилки.

**Q: Як створити новий проект з секціями?**
A: Не потрібно писати код! Просто:
   1. Логін в адмінку
   2. Створіть новий проект
   3. Перейдіть в "Manage Sections"
   4. Додайте потрібні секції
   - Новий проект автоматично буде мати секції

**Q: Чому міграція не працює?**
A: Переконіть:
   - Супabase URL правильна в .env?
   - SUPABASE_SERVICE_ROLE_KEY починається з `sb_secret_`?
   - Маєте права адміністратора в Supabase?

**Q: Як виправити помилку міграції?**
A: 
   - Зробіть `ROLLBACK` в Supabase SQL Editor
   - Або видаліть колонку `sections` зново:
     ```sql
     ALTER TABLE projects DROP COLUMN sections;
     ```
   - Потім запустіть міграцію заново

---

## 📞 Документація

### Основні документи

1. **Deployment Verification**: `docs/deployment/deployment-verification.md` (англійською, українською!)
2. **Implementation Plan**: `docs/implementation/dynamic-product-page-plan.md` (англійською)
3. **Укранїнська інструкція**: `docs/deployment/інструкція-міграція-динамічної-сторінки.md` (цей файл)

---

## ✅ Підсумок

### Міграції:
- ✅ SQL міграція бази створена (`003-add-project-sections.sql`)
- ✅ Міграційний скрипт створено (`scripts/migrate-project-sections.ts`)

### Реалізація:
- ✅ TypeScript типи для 10 секцій
- ✅ Universal Section Renderer
- ✅ 10+ компонентів секцій
- ✅ Адмін-панель для керування секціями
- ✅ Translation support

### Готово до тестування:
- ✅ Інструкція створена
- ✅ Посилання на документацію створено
- ✅ Відповідні готові для деплою

---

## 🚀 Наступні дії (для вас!)

### 1. **Запустіть міграцію бази** (5 хв)
   - Supabase Dashboard → SQL Editor → RUN

### 2. **Запустіть міграцію даних** (2 хв)
   - `npx tsx scripts/migrate-project-sections.ts`

### 3. **Протестуйте адмін-панель** (10 хв)
   - Логін → Dashboard → Manage Sections → Тестуйте функції

### 4. **Тестуйте сторінку проектів** (5 хв)
   - Перейдіть на http://localhost:5173/project/[id]
   - Перевірте, що секції відображаються

### 5. **Збережіть і задеплойте** (15 хв)
   - Deploy to production
   - Verify everything works live

---

**Час до деплою**: ~35 хвилин

**Статус**: ✅ **ГОТОВИЙ ДЕПЛОЮ**!

---

**Документ версія**: 1.0
**Останнійй час**: 2026-02-07
**Мова**: 🇺🇦

**Успішних виконань!** 🎉
