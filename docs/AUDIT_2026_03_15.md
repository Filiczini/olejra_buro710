# Buro 710 — Full Project Audit

**Дата:** 2026-03-15
**Scope:** React 19 + Vite + Tailwind v4 + Supabase + Node/Express

---

## 1. ПРОДУКТИВНІСТЬ КОДУ

### Мертвий код

| # | Проблема | Файл | Пріоритет |
|---|----------|------|-----------|
| 1.1 | `HeroSectionForm.tsx` ніде не імпортується (використовується `PostHeroForm`) | `frontend/src/components/admin/HeroSectionForm.tsx` | high |
| 1.2 | `HeroSectionPreview.tsx` ніде не імпортується (використовується `PostHeroPreview`) | `frontend/src/components/admin/HeroSectionPreview.tsx` | high |
| 1.3 | `UsersPage.tsx` і `SettingsPage.tsx` — заглушки без функціоналу | `frontend/src/pages/admin/` | low |
| 1.4 | Дублікат seed файлу `seed-posts.ts` vs `seed-posts-new.ts` | `backend/src/` | low |

### Дублювання логіки

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 1.5 | Транслітерація дублюється: `frontend/src/utils/transliterate.ts` і `backend/src/services/postService.ts:47-95` | Винести в `shared/` package | medium |
| 1.6 | Однаковий fetch-патерн в 3 компонентах: `ProjectsPage`, `FeaturedPostsSection`, `ProjectsGallerySection` — `.then().catch().finally()` | Винести в кастомний хук `useFetchPosts()` | medium |

### console.log без logger

| # | Проблема | Файл:рядок | Пріоритет |
|---|----------|-----------|-----------|
| 1.7 | `console.error` замість `logger.error` | `frontend/src/components/sections/HeroSlider.tsx:19` | high |
| 1.8 | `console.log` замість `logger.info` | `backend/src/index.ts:90` | high |

### Типізація

| # | Проблема | Файл:рядок | Пріоритет |
|---|----------|-----------|-----------|
| 1.9 | `any` в multer fileFilter | `backend/src/middleware/multer.ts:9` | medium |
| 1.10 | `any` в activityLogService fallback | `backend/src/services/activityLogService.ts:80,83` | medium |
| 1.11 | `req as any` для доступу до `.user` | `backend/src/middleware/requestId.ts:28` | medium |
| 1.12 | `as unknown as { ... }` подвійний каст (анти-патерн) | `backend/src/routes/posts.ts:150`, `backend/src/routes/api/posts.ts:230` | medium |

---

## 2. PERFORMANCE

### Bundle & Code Splitting

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 2.1 | Адмін-сторінки (5 шт) завантажуються в основний бандл для ВСІХ юзерів | `React.lazy()` + `Suspense` для `/admin/*` роутів | high |
| 2.2 | Bundle > 500KB (Vite warning), немає chunk splitting | Додати `build.rollupOptions.output.manualChunks` | medium |

### Зображення

| # | Проблема | Файл:рядок | Пріоритет |
|---|----------|-----------|-----------|
| 2.3 | `ProjectsPage` — images без `loading="lazy"` | `frontend/src/pages/ProjectsPage.tsx:67` | high |
| 2.4 | `HeroSlider` hero image без `loading="lazy"` (але це above-the-fold, lazy НЕ потрібен) | `frontend/src/components/sections/HeroSlider.tsx:65` | skip |

### Ре-рендери

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 2.5 | `BlockRenderer` не обгорнутий в `React.memo` | Додати memo | medium |
| 2.6 | Inline handlers в циклах: `GalleryUploader` (165,166,184), `BlockItem` (70,78,86,104) | Винести в useCallback або передавати id | low |
| 2.7 | Scroll handler без debounce в `PostGalleryBlock:99` | Додати debounce | low |

### Supabase запити

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 2.8 | N+1 query: при створенні поста, для кожного block upload окремий SELECT + UPDATE | Prefetch blocks один раз, потім оновлювати в пам'яті | high |
| 2.9 | `blockService.syncBlocks()` — update блоків послідовний `for...of` з await | Замінити на `Promise.all` | high |
| 2.10 | `blockService.syncBlocks()` — зайвий refetch після sync | Повертати дані з операцій, не робити повторний SELECT | medium |

### Мережа

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 2.11 | `getFeatured()` викликається ДВІЧІ на головній (FeaturedPostsSection + ProjectsGallerySection) | Підняти fetch в батьківський компонент або кешувати | high |
| 2.12 | Жоден async запит не використовує `AbortController` — можливі state updates після unmount | Додати abort signal в useEffect cleanup | medium |

---

## 3. АРХІТЕКТУРА

### God Components

| # | Проблема | Рядків | Рекомендація | Пріоритет |
|---|----------|--------|-------------|-----------|
| 3.1 | `EditPostPage.tsx` — форма + валідація + файли + блоки в одному компоненті | 392 | Винести хуки: `usePostForm()`, `useBlockFiles()` | medium |
| 3.2 | `ActivityLogPage.tsx` — фільтри + таблиця + пагінація | 310 | Винести фільтри і таблицю в окремі компоненти | low |
| 3.3 | `PostsPage.tsx` — пошук + фільтр + таблиця | 285 | Пагінацію вже винесено, решта прийнятна | low |

### Магічні числа

| # | Проблема | Файл | Пріоритет |
|---|----------|------|-----------|
| 3.4 | Pagination defaults `{ page: 1, limit: 10 }` хардкоджені в кожному компоненті | `PostsPage.tsx:13`, `ActivityLogPage.tsx` | low |
| 3.5 | Slider interval `6000`ms хардкоджений | `HeroSlider.tsx:31` | low |
| 3.6 | Контактна інформація хардкоджена в компоненті (email, телефон, адреса) | `ContactPage.tsx:44-87` | low |

---

## 4. БЕЗПЕКА

### Валідація

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 4.1 | DELETE gallery endpoint приймає `image_url` з body БЕЗ валідації | Додати Zod валідацію URL формату | high |

### Rate Limiting

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 4.2 | `/api/v1/posts` (external API) — немає rate limiting | Додати rate limit 100 req/min | high |
| 4.3 | `/api/posts` (admin CRUD) — немає rate limiting | Додати rate limit per-user | medium |
| 4.4 | `/api/logs` — немає rate limiting | Додати rate limit | low |

### CSP

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 4.5 | `contentSecurityPolicy: false` в Helmet | Увімкнути CSP з whitelist для Swagger CDN | medium |

### Інше

| # | Проблема | Статус |
|---|----------|--------|
| JWT auth | Працює коректно, 24h expiration | OK |
| API Key | Timing-safe comparison | OK |
| CORS | Обмежено FRONTEND_URL | OK |
| File uploads | MIME + розмір + кількість валідуються | OK |
| SQL injection | Supabase client, параметризовані запити | OK |
| XSS | React JSX escaping, немає dangerouslySetInnerHTML | OK |
| Secrets | Все в .env, нічого в коді | OK |

---

## 5. UX/UI

### Loading стани

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 5.1 | `FeaturedPostsSection` — при помилці просто не рендериться, без feedback | Додати error state або retry | low |
| 5.2 | `ProjectsGallerySection` — аналогічно | Додати error state | low |

### Error стани

| # | Проблема | Рекомендація | Пріоритет |
|---|----------|-------------|-----------|
| 5.3 | `ProjectsPage` — catch логує помилку, але не показує UI | Додати error banner | medium |
| 5.4 | `HeroSlider` — catch логує, але показує loading forever | Додати fallback UI | medium |

### Accessibility

| # | Проблема | Файл | Пріоритет |
|---|----------|------|-----------|
| 5.5 | HeroSlider prev/next кнопки без `aria-label` | `HeroSlider.tsx:114-133` | high |
| 5.6 | ProjectsPage project links без `aria-label` | `ProjectsPage.tsx:59-62` | medium |
| 5.7 | `substring(7)` для Bearer token — крихкий парсинг | `backend/src/middleware/auth.ts:18` | low |

---

## ЗВЕДЕНА ТАБЛИЦЯ ПРІОРИТЕТІВ

### HIGH (впроваджено ✅)

| # | Категорія | Проблема | Статус |
|---|-----------|----------|--------|
| 1.1 | CODE | Мертвий компонент `HeroSectionForm.tsx` | ✅ Видалено |
| 1.2 | CODE | Мертвий компонент `HeroSectionPreview.tsx` | ✅ Видалено |
| 1.7 | CODE | `console.error` в HeroSlider | ✅ Замінено на logger |
| 1.8 | CODE | `console.log` в index.ts | ✅ Замінено на logger |
| 2.1 | PERF | Admin pages не code-split | ✅ React.lazy() + Suspense |
| 2.3 | PERF | ProjectsPage images без lazy | ✅ Додано loading="lazy" |
| 2.8 | PERF | N+1 query в block upload (create route) | ✅ Prefetch + Map |
| 2.9 | PERF | Sequential block updates в syncBlocks | ✅ Promise.all |
| 2.11 | PERF | Duplicate getFeatured() на головній | ✅ Не актуально (FeaturedPostsSection не на HomePage) |
| 4.1 | SEC | DELETE gallery без валідації body | ✅ URL format validation |
| 4.2 | SEC | External API без rate limiting | ✅ 100 req/min |
| 5.5 | UX | HeroSlider кнопки без aria-label | ✅ Додано aria-label |

### MEDIUM (впроваджено ✅)

| # | Категорія | Проблема | Статус |
|---|-----------|----------|--------|
| 1.5 | CODE | Транслітерація дублюється frontend/backend | ✅ Винесено в `@buro710/shared` |
| 1.6 | CODE | Дублікат fetch-патерну в 3 компонентах | ✅ Створено `useFetchPosts()` хук |
| 1.9 | CODE | `any` в multer fileFilter | ✅ Типізовано `unknown` + `Express.Multer.File` |
| 1.10 | CODE | `any` в activityLogService fallback | ✅ Типізовано `{ user_email: string }` |
| 1.11 | CODE | `req as any` в requestId.ts | ✅ Використано Express augmented type |
| 1.12 | CODE | Подвійні касти `as unknown as` | ✅ Частково: `type` каст виправлено через `BlockType`, `data` каст залишено (JSON boundary) |
| 2.2 | PERF | Bundle chunk splitting | ✅ `manualChunks` в vite.config.ts |
| 2.5 | PERF | BlockRenderer без memo | ✅ Додано `React.memo` |
| 2.10 | PERF | Зайвий refetch в syncBlocks | ✅ Повертає дані з операцій |
| 2.12 | PERF | Відсутній AbortController | ✅ Cancelled flag в useEffect cleanup |
| 3.1 | ARCH | EditPostPage — god component | ✅ Винесено `usePostForm()` хук |
| 4.3 | SEC | Admin endpoints без rate limiting | ✅ 200 req/min |
| 4.5 | SEC | CSP вимкнений | ✅ Увімкнено з whitelist для Swagger CDN |
| 5.3-5.4 | UX | Відсутній error UI в ProjectsPage і HeroSlider | ✅ Error banner + fallback UI |
| 5.6 | UX | ProjectsPage links без aria-label | ✅ Додано aria-label |

### LOW (впроваджено ✅)

| # | Категорія | Проблема | Статус |
|---|-----------|----------|--------|
| 1.3 | CODE | Заглушки UsersPage/SettingsPage | ⏭️ Прийнятно — інтенційні placeholder'и |
| 1.4 | CODE | Дублікат seed файлу | ✅ Видалено `seed-posts-new.ts` |
| 2.6 | PERF | Inline handlers в циклах | ⏭️ Прийнятно — передають аргументи в memoized callbacks |
| 2.7 | PERF | Scroll handler без debounce | ✅ Додано debounce 50ms |
| 3.2 | ARCH | ActivityLogPage god component | ⏭️ Прийнятно — переважно render логіка |
| 3.3 | ARCH | PostsPage god component | ⏭️ Прийнятно — пагінацію вже винесено |
| 3.4 | ARCH | Pagination defaults хардкоджені | ✅ Винесено в константи |
| 3.5 | ARCH | Slider interval хардкоджений | ✅ Винесено в `SLIDE_INTERVAL_MS` |
| 3.6 | ARCH | Контакти хардкоджені | ✅ Винесено в `CONTACT_INFO` / `SOCIAL_LINKS` |
| 4.4 | SEC | Rate limit на logs | ✅ 200 req/min |
| 5.1 | UX | FeaturedPostsSection без error state | ✅ Graceful hide on error |
| 5.2 | UX | ProjectsGallerySection без error state | ✅ Graceful hide on error |
| 5.7 | UX | Bearer token `substring(7)` | ✅ Замінено на `split(' ')[1]` |
