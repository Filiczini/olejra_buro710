# Інструкція: Міграція динамічної сторінки продукту

**Статус**: ✅ Готовий до виконання
**Мова**: 🇺🇦

---

## 📋 Що реалізовано

### ✅ Повністю динамічна сторінка продукту
- ✅ **Hero секція тепер повністю динамічна**:
  - Картинка: змінюється через адмінку
  - Текст (title, subtitle, short_description): редагується
  - Layout: centered/left/right/split
  - Animation: zoom/fade/slide/none
  - CTA кнопка: опціонально

- ✅ **10 типів секцій доступні**:
  - Hero (головна секція)
  - Metadata (дані проекту)
  - About (про про проект)
  - Full-width image (повноекранна картинка)
  - Concept (концепція з галереєю)
  - Design Zones (зони проекту з 4 layout-ами)
  - Text Block (текстовий блок)
  - Image Block (картинка з підписом)
  - Gallery (галерея зі слайдером)
  - CTA (кнопка дії)
  - Tags (теги як pills)

- ✅ **Універсальний Section Renderer**
- ✅ **Адмін-панель для керування секціями**
- ✅ **Підтримка перекладів**
- ✅ **Backward compatibility** для існуючих проектів

---

## 🚀 Покрокова інструкція

### Крок 1: Міграція бази даних (5 хв)

#### 1.1 Зайдіть Supabase Dashboard

Перейдіть за URL:
```
https://supabase.com/dashboard/project/zrcaowbpewulytlwnbnb
```

#### 1.2 Відкрийте SQL Editor

У меню знайдіть:
- **SQL Editor** (або натисніть комбінацію `Shift+Q`)

#### 1.3 Запустіть міграцію

1. Відкрийте файл: `003-add-project-sections.sql`
2. Скопіюйте весь вміст файлу (Ctrl+A)
3. Вставте в SQL Editor
4. Натисніть **RUN** (або клавіша `F5`)

#### 1.4 Перевірте результат

Після запуску ви повинні побачити:
```
Success. No rows returned.
```

Це означає успіх!

#### 1.5 Опціональна перевірка

В SQL Editor виконіть:
```sql
SELECT id, title, sections
FROM projects
LIMIT 5;
```

Перевірте:
- Колонка `sections` існує
- Значення: `[]` (пустий масив)

---

### Крок 2: Міграція даних існуючих проектів (2 хв)

#### 2.1 Переконьте credentials

Переконьте, що у файлі `.env` є:
```
SUPABASE_URL=https://...
SUPABASE_SERVICE_ROLE_KEY=sb_secret_...
```

#### 2.2 Запустіть міграційний скрипт

У терміналі виконіть:
```bash
npx tsx scripts/migrate-project-sections.ts
```

Скрипт автоматично:
- Завантажує всі проекти
- Створить секції для кожного проекту
- Зберігає існуючі дані (не видаляє!)

#### 2.3 Очікуваний результат

```
🚀 Starting project sections migration...

📊 Found 22 projects to migrate

🔄 Migrating project: project-id-1
  ✅ Added hero section
  ✅ Added metadata section
  ✅ Added about section
  ✅ Added full-width image section
  ✅ Added concept section
✅ Successfully migrated project-id-1 with 6 sections

... (повторюється для всіх проектів)

============================================================
📈 МІГРАЦІЯ СУММА
============================================================
Total projects: 22
✅ Successfully migrated: 22
❌ Failed to migrate: 0
============================================================

✅ All projects migrated successfully!
```

**Що створено для кожного проекту**:
1. **Hero секція** (order: 0)
   - Вміщує title, subtitle, short_description, image_url
   - Заповнений з існуючих даних

2. **Metadata секція** (order: 1)
   - Вміщує architects, area, location, year, photo_credits

3. **About секція** (order: 2)
   - Вміщує title, description, icon

4. **Full-width image секція** (order: 3)
   - Вміщує image_url, caption, alt

5. **Concept секція** (order: 4)
   - Вміщує heading, caption, quote, images, features

6. **Design zones** (order: 5)
   - Зберігає всі існуючі design_zones

---

### Крок 3: Тестування адмін-панелі (10 хв)

#### 3.1 Логін в адмін-панель

Перейдіть:
```
http://localhost:5173/admin/login
```

Credentials:
- Email: `admin@example.com`
- Пароль: `admin123`

#### 3.2 Перейдіть до списку проектів

```
http://localhost:5173/admin/dashboard
```

#### 3.3 Відкрийте "Manage Sections"

Натисніть кнопку **"📋 Manage Sections"** на картці проекту.

#### 3.4 Тестуйте додавання секцій

1. Натисніть тип секції (наприклад, "Hero Section")
2. Натисніть "Edit"
3. Заповніть контент (title, subtitle, description)
4. Натисніть "Close"
5. Перш ніж "Save Changes"!

#### 3.5 Тестуйте редагування

1. Натисніть "Edit" на існуючій секції
2. Змініть контент
3. Натисніть "Close"
4. Перш ніж "Save Changes"!

#### 3.6 Тестуйте видалення

1. Натисніть "Delete"
2. Підтвердіть у діалоговому вікні
3. Перш ніж зберегти зміни ("Save Changes")

#### 3.7 Тестуйте зміну порядку

1. Натисніть **↑** або **↓** біля секції
2. Змініть порядок кілька разів
3. Збережіть зміни ("Save Changes")
4. Перевірте порядок на сторінці проекту

#### 3.8 Тестуйте toggle visibility

1. Натисніть кнопку **"Visible" / "Hidden"**
2. Збережіть зміни ("Save Changes")
3. Перевіть сторінку проекту (прихована секція не відображається)

---

## 🎯 Що ви тепер можете робити

### Додавати нові секції без коду

Раніше:
- ❌ Потрібно було писати код
- ❌ Потрібно було редагувати components
- Тільки **ТЕПЕР**:

1. **Логін в адмінку**
2. **Натиснути "Add New Section"**
3. **Вибрати тип секції** (10 типів!)
4. **Заповнити контент**
5. **Зберегти**

### Редагувати Hero секцію

Раніше:
- ❌ Треба було редагувати `HeroSection.tsx`
- ❌ Треба було перезбілдити бекенд
- Тільки **ТЕПЕР**:

1. **Натиснути "Edit" на Hero секції**
2. **Змінити title**
3. **Змінити subtitle**
4. **Завантажити нову картинку** (через Supabase Storage або вставити URL)
5. **Змінити layout** (centered/left/right/split)
6. **Збергити**

### Змінювати порядок секцій

Раніше:
- ❌ Потрібно було змінювати `order` поля в коді
- Тільки **ТЕПЕР**:

1. **Натиснути ↑ ↓ біля секції**
2. **Змінити порядок кілька разів**
3. **Збергити**

### Приховати/показувати секції

Раніше:
- ❌ Потрібно було видаляти компоненти
- Тільки **ТЕПЕР**:

1. **Натиснути кнопку "Visible/Hidden"**
2. **Збергити**

---

## 📊 Архітектура системи

### Як це працює

```
Admin Panel
     │
     ├─► POST /api/portfolio/:projectId/sections
     │    │
     │    ▼─► Supabase Database
     │         │
     │    ├─► projects.sections (JSONB array)
     │    └─► [{ type: 'hero', order: 0, ... }, ...]
     │
     │
     └─► Response: ✅ Updated!
     
Project Page
     │
     ├─► GET /api/portfolio/:id
     │    │
     │    ▼─► Supabase Database
     │         │
     │    └─► { id, title, sections: [...] }
     │         │
    │            │
     │            ▼─► SectionRenderer(sections)
     │                   │
     │                   ├─► sections.map(section => {
     │                   │   └─► Component[section.type](content)
     │                   │              })
     │                   │
     └─► Rendered! ✅
```

---

## 🎯 Доступні типи секцій

| Тип | Назва | Опис | Layout |
|-----|-------|------|--------|
| `hero` | Hero Section | Головна екрана з картинкою, текстом, анімацією | Full screen |
| `metadata` | Metadata | Дані про проект (архітектори, площа, рік тощо) | Grid |
| `about` | About | Текстовий блок з описом проекту | Centered |
| `full-width-image` | Full Width Image | Повноекранна картинка на всю ширину | Full width |
| `concept` | Concept | Концепція дизайну з галереєю зображень | Two-column |
| `design-zones` | Design Zones | Зони проекту з різними layout-ами | Multiple |
| `text-block` | Text Block | Заголовок + абзаци | Centered |
| `image-block` | Image Block | Картинка з підписом | Full width |
| `gallery` | Gallery | Галерея/слайдер з зображень | Grid/Slider |
| `cta` | CTA | Кнопка заклику до дії | Centered |
| `tags` | Tags | Теги проекту як pills | Horizontal |

---

## 🔧 Троублешутин

### Помилка: "Sections don't show on project page"

**Причина**: Секції не мігровані або не збережені

**Рішення**:
1. Перевірте, чи виконали міграції (Кроки 1-2)
2. Перезавантажте проект: http://localhost:5173/project/[project-id]
3. Перевірте, чи секції збережені: "Save Changes"

### Помилка: "Admin page not found"

**Причина**: Неправильний URL

**Рішення**:
- Правильний URL: `/admin/projects/:projectId/sections`
- Наприклад: `/admin/projects/6e11d6c3-3304-466f.../sections`

### Помилка: "Cannot add section"

**Причина**: Секція з такою назвою вже існує

**Рішення**:
- Змініть назву або видаліть існуючу секцію

### Помилка: "Migration script fails"

**Причина**: Неправильні credentials в .env

**Рішення**:
```bash
# Перевірте credentials
cat .env | grep SUPABASE

# Переконьте, що service role key починається з: sb_secret_
```

---

## ✅ Перевірте успіш міграції

### Тест 1: API працює

```bash
curl http://localhost:3000/api/portfolio
```

Повинно: JSON з масивом проектів?

### Тест 2: Секції доступні

```bash
curl http://localhost:3000/api/portfolio/[project-id]/sections
```

Повинно: JSON з секціями?

### Тест 3: Адмін-панель доступна

```bash
# Перейдіть в браузер
http://localhost:5173/admin/login
# Login: admin@example.com / admin123
```

Можете увійти в адмінку?

---

## 📝 Наступні кроки після деплою

### Короткострокові
- [ ] Додати нові секції до проектів
- [ ] Редагувати Hero секції
- [ ] Створити нові сторінки з унікальними секціями
- [ ] Додати переклади (якщо потрібно)

### Середньострокові
- [ ] Розширити редактор для Design Zones (замість JSON)
- [ ] Розширити редактор для Gallery
- [ ] Інтегрувати ImageUpload компонент
- [ ] Додати drag-and-drop для секцій
- [ ] Додати preview mode в адмінку

---

## 📞 Підтримка

### Як отримати допомогу

1. **Документація**: `docs/deployment/deployment-verification.md` (англійською)
2. **План**: `docs/implementation/dynamic-product-page-plan.md` (англійською)
3. **Інструкція**: Цей файл!

### Часті запитання

**Q: Де бачу список секцій?**
A: http://localhost:5173/admin/projects/:id/sections

**Q: Як додати нову секцію?**
A: Кнопка "Add New Section" → виберіть тип → заповніть контент → Save

**Q: Як видалити секцію?**
A: Кнопка "Delete" → підтверджте → Save

**Q: Як змінити порядок?**
A: Кнопки ↑ ↓ біля секції → Save

**Q: Як приховати секцію?**
A: Кнопка "Visible" → "Hidden" → Save

**Q: Де редагувати Hero секцію?**
A: Натисніть "Edit" на Hero секції в списку → змініть контент → Save

---

## 🎉 Вітаю! Система готова до використання!

Ви тепер маєте повно функціональну CMS для керування всім контентом сторінки продукту без написання коду.

**Що може робити адміністратор контенту**:
- ✅ Додавати секції
- ✅ Редагувати контент
- ✅ Змінювати порядок
- ✅ Приховати/показувати
- ✅ Зберігати зміни

**Все через адмін-панель, без коду!** 🚀

---

**Документ створено**: 2026-02-07
**Версія**: 1.0
**Статус**: ✅ Ready for Deployment
